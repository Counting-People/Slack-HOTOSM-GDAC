text = (
        f"{emoji} *GDACS {alert} Alert: {props['name']}*\n"
        f"*Country:* {props['country']}\n"
        f"*Event ID:* {props['eventid']}\n"
        f"*From:* {props.get('fromdate', 'unknown')[:10]}  *To:* {props.get('todate', 'unknown')[:10]}\n"
        f"*Last updated:* {props.get('datemodified', 'unknown')[:10]}\n"
        f"*Details:* {props['description'][:200]}\n"
        f"*More info:* https://www.gdacs.org/report.aspx?eventid={props['eventid']}&episodeid={props.get('episodeid', '')}&eventtype={props.get('eventtype', '')}"
    )

That is likely the best move. If the current webhook is tied to an existing Slack Workflow that you didn't create, you won't be able to see or edit the variable mapping (the "schema") it expects. Those empty parentheses () are a classic sign that the workflow is receiving a payload but can't find the specific keys (like title or event_id) it was told to look for.

When you reach out to your Slack Admin, here is how to get this resolved quickly:

Option 1: The "Clean Slate" (Recommended)
Ask the Admin to create a standard Incoming Webhook for you.

Why: Standard webhooks are "dumb"—they just post whatever text or blocks you send them without needing a predefined schema.

The Request: "Can you create a standard Incoming Webhook for the [Channel Name] channel? I need one that accepts a standard JSON payload so I can send formatted alerts from a Python script."

Option 2: Fixing the Current Workflow
If they insist on using the existing Workflow, they need to check the "Receive a Webhook" step.

The Issue: The workflow is likely configured to expect specific variable names.

The Fix: Ask them: "What are the exact variable names configured in the 'Receive a Webhook' step? For example, is it 'text', 'title', 'ID', or something else?"

Once you have those names: We can simply update your Python dictionary to match them.

How to test once you have the new Webhook
Once they give you a new URL, update your GitHub Secret named SLACK_WEBHOOK_URL.

Your current script is already generating the correct data, so as soon as you have a "standard" webhook that doesn't filter your input, those alerts should start appearing perfectly in Slack.

Do you want me to draft a quick technical snippet you can send to your Admin to show them exactly what the script is trying to post?